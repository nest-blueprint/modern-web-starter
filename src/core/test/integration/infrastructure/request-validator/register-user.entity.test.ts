describe('[Core/Infrastructure/Request] CreateCustomerProfileEntity', () => {
  test('Request body, without the required fields', () => {});

  test('Request body, with the empty body', () => {});

  test('Request body, with invalid data fields', () => {});

  test('Request body, correctly formated and set', () => {});
});
